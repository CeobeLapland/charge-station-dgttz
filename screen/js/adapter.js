(function(){"use strict";const arr=v=>Array.isArray(v)?v:[],num=(v,d=0)=>Number.isFinite(Number(v))?Number(v):d;const metricKeys=["station_count","charger_count","online_charger_count","today_energy_kwh","today_orders","today_revenue"];
const metrics=v=>Object.fromEntries(metricKeys.map(k=>[k,num(v?.[k])]));const alarm=a=>({id:a.id??`${a.station_id}-${a.occur_time}`,occur_time:a.occur_time??"",station_name:String(a.station_name??"未知站点"),charger_code:String(a.charger_code??a.charger_id??"--"),content:String(a.content??a.message??a.type??"未知告警"),level:a.level??"info"});
function snapshot(p={}){return{metrics:metrics(p.metrics),status_distribution:{idle:num(p.status_distribution?.idle),charging:num(p.status_distribution?.charging),offline:num(p.status_distribution?.offline),fault:num(p.status_distribution?.fault)},order_trend:arr(p.order_trend),energy_revenue_trend:arr(p.energy_revenue_trend),station_rank:arr(p.station_rank),stations:arr(p.stations),alarms:arr(p.alarms).map(alarm),events:arr(p.events),filter_options:{regions:arr(p.filter_options?.regions),stations:arr(p.filter_options?.stations)}}}function message(raw){const m=typeof raw==="string"?JSON.parse(raw):raw;if(!m||typeof m!=="object"||typeof m.type!=="string")throw new Error("服务端消息格式错误");return m}const partialMetrics=v=>Object.fromEntries(metricKeys.filter(k=>v&&Object.hasOwn(v,k)).map(k=>[k,num(v[k])]));window.ScreenAdapter={normalizeSnapshot:snapshot,normalizeMessage:message,normalizeAlarm:alarm,normalizeMetrics:metrics,normalizePartialMetrics:partialMetrics};}());
